# 🚀 Инструкция по деплою на IONOS (kas.all-link.com)

## ⚠️ ВАЖНО: Замена домена перед деплоем

**Текущий домен в проекте**: `ass-security.de` (временный)

Когда получите финальный домен от клиента, выполните замену:

### Способ 1: Через VS Code (рекомендуется)

1. Нажмите `Cmd+Shift+F` (или `Ctrl+Shift+F` на Windows)
2. В поле "Search" введите: `ass-security.de`
3. В поле "Replace" введите новый домен (например: `new-domain.de`)
4. Нажмите "Replace All" (заменить все)
5. Сохраните все файлы (`Cmd+K S` или `Ctrl+K S`)

### Способ 2: Через терминал

```bash
# Замените NEW_DOMAIN на ваш новый домен
find . -type f \( -name "*.html" -o -name "*.tsx" -o -name "*.xml" -o -name "*.txt" \) -exec sed -i '' 's/ass-security.de/NEW_DOMAIN/g' {} +
```

### Файлы, которые будут обновлены:

- ✅ `index.html` (мета-теги, Schema.org)
- ✅ `public/sitemap.xml` (все URL)
- ✅ `public/robots.txt` (ссылка на sitemap)
- ✅ `src/components/Footer.tsx` (email)
- ✅ `src/components/header/MobileMenu.tsx` (email)
- ✅ `src/components/contact/ContactInfo.tsx` (email)

### После замены:

```bash
npm run build  # Пересоберите проект
```

---

## 📦 Подготовка файлов

Проект уже собран и готов к деплою. Папка `dist/` содержит все необходимые файлы.

---

## 📋 Шаг 1: Подключение к IONOS

1. Откройте браузер и перейдите на: **https://kas.all-link.com**
2. Войдите в систему с вашими учетными данными IONOS
3. Перейдите в раздел **"Webspace"** или **"FTP-Zugang"**

---

## 📤 Шаг 2: Загрузка файлов через FTP

### Вариант A: Использование FileZilla (рекомендуется)

1. **Скачайте FileZilla Client**: https://filezilla-project.org/download.php?type=client
2. **Откройте FileZilla** и введите данные FTP из IONOS панели:

   - **Host**: `ftp.ihr-domain.de` (или IP-адрес из панели IONOS)
   - **Username**: ваш FTP логин
   - **Password**: ваш FTP пароль
   - **Port**: `21` (обычный FTP) или `22` (SFTP)

3. **Подключитесь** к серверу

4. **Найдите корневую директорию сайта**:

   - Обычно это `/` или `/html` или `/public_html`
   - В IONOS это часто просто корневая папка `/`

5. **Загрузите ВСЕ файлы из папки `dist/`**:
   - Выберите все файлы в локальной папке `dist/`
   - Перетащите их в корневую директорию на сервере
   - Убедитесь, что `.htaccess` файл тоже загружен!

### Вариант B: Использование встроенного File Manager в IONOS

1. В панели IONOS найдите **"File Manager"** или **"Datei-Manager"**
2. Откройте корневую директорию сайта
3. Загрузите все файлы из папки `dist/` через интерфейс
4. Убедитесь, что `.htaccess` файл загружен (он может быть скрыт)

---

## ✅ Шаг 3: Проверка структуры файлов

После загрузки структура на сервере должна выглядеть так:

```
/ (корневая директория)
├── .htaccess              ← ВАЖНО! Должен быть загружен
├── index.html
├── robots.txt
├── sitemap.xml
├── logotip-dark.svg
├── logotip-light.png
├── process.js
├── process.wasm
├── scene.splinecode
├── assets/
│   ├── index-DKajHJDy.css
│   ├── FAQ-BwOaWvOm.js
│   ├── ui-components-b7NkNkdZ.js
│   └── ... (другие JS/CSS файлы)
└── favicon/
    ├── favicon.ico
    └── ... (другие favicon файлы)
```

---

## 🔧 Шаг 4: Настройка домена

1. В панели IONOS перейдите в **"Domains"**
2. Убедитесь, что ваш домен указывает на правильный Webspace
3. Проверьте настройки DNS (обычно настроены автоматически)

---

## 🔒 Шаг 5: Включение SSL/HTTPS

1. В панели IONOS найдите **"SSL-Zertifikat"** или **"SSL Certificate"**
2. Активируйте бесплатный SSL сертификат от Let's Encrypt
3. Подождите 5-10 минут для активации
4. `.htaccess` файл автоматически перенаправит HTTP → HTTPS

---

## 🧪 Шаг 6: Тестирование

1. Откройте браузер и перейдите на ваш домен: **https://ваш-домен.de**
2. Проверьте, что сайт загружается корректно
3. Проверьте все секции:
   - ✅ Главная страница
   - ✅ Услуги (#services)
   - ✅ О нас (#about)
   - ✅ Команда (#team)
   - ✅ Контакты (#contact)
4. Проверьте SEO файлы:
   - ✅ https://ваш-домен.de/robots.txt
   - ✅ https://ваш-домен.de/sitemap.xml

---

## 🐛 Возможные проблемы и решения

### Проблема 1: Сайт не загружается (404 ошибка)

**Решение**:

- Убедитесь, что файл `index.html` находится в корневой директории
- Проверьте права доступа к файлам (должны быть 644 для файлов, 755 для папок)

### Проблема 2: .htaccess не работает

**Решение**:

- Убедитесь, что файл `.htaccess` загружен (он может быть скрыт)
- Проверьте, что Apache mod_rewrite включен (обычно включен в IONOS)
- Проверьте права доступа: `chmod 644 .htaccess`

### Проблема 3: Стили не применяются

**Решение**:

- Проверьте, что папка `assets/` загружена полностью
- Очистите кэш браузера (Ctrl+Shift+R или Cmd+Shift+R)
- Проверьте консоль браузера (F12) на наличие ошибок

### Проблема 4: HTTP не перенаправляется на HTTPS

**Решение**:

- Убедитесь, что SSL сертификат активирован в панели IONOS
- Проверьте, что `.htaccess` файл загружен с правилами редиректа
- Подождите 10-15 минут после активации SSL

---

## 📊 Оптимизация после деплоя

### 1. Проверка производительности

- Google PageSpeed Insights: https://pagespeed.web.dev/
- GTmetrix: https://gtmetrix.com/

### 2. Регистрация в Google Search Console

1. Перейдите на: https://search.google.com/search-console
2. Добавьте свой сайт
3. Отправьте sitemap: `https://ваш-домен.de/sitemap.xml`

### 3. Мониторинг

- Проверяйте логи в панели IONOS
- Настройте Google Analytics (если нужно)

---

## 📞 Поддержка IONOS

- **Телефон**: +49 721 170 5555
- **Email**: info@ionos.de
- **Панель управления**: https://kas.all-link.com

---

## ✅ Чеклист деплоя

- [ ] Получен финальный домен от клиента
- [ ] Выполнена замена `ass-security.de` на новый домен
- [ ] Проект пересобран: `npm run build`
- [ ] Файлы из `dist/` загружены на сервер
- [ ] `.htaccess` файл загружен и работает
- [ ] SSL сертификат активирован
- [ ] Сайт открывается по HTTPS
- [ ] Все секции работают корректно
- [ ] robots.txt доступен
- [ ] sitemap.xml доступен
- [ ] Нет ошибок в консоли браузера
- [ ] Сайт зарегистрирован в Google Search Console

---

## 🎉 Готово!

Ваш сайт **A.S.S Security UG** готов к работе!

Удачи! 🚀
